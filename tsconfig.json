{
  "// note": "Type checking only. Nothing is emitted: the browser loads the .js files directly, which keeps the app buildless on a phone. `npm run typecheck` is the compiler pass that would otherwise never happen.",
  "compilerOptions": {
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "bundler",
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "allowJs": true,
    "checkJs": true,
    "noEmit": true,
    "strict": false,
    "strictNullChecks": true,
    "noImplicitThis": true,
    "noUnusedLocals": true,
    "noUnusedParameters": false,
    "noFallthroughCasesInSwitch": true,
    "forceConsistentCasingInFileNames": true,
    "skipLibCheck": true,
    "resolveJsonModule": true
  },
  "include": ["static/js/**/*.js"],
  "exclude": ["node_modules", "tests"],
  "// tests": "Deliberately not typechecked. They assert a value then index it, which strictNullChecks flags everywhere for no benefit — and unlike the app, they are verified by being executed on every CI run."
}
