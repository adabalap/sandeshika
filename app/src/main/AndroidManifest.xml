<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!--
      READ_SMS is the whole product. Play Protect will hard-block a sideload
      that declares it: use `adb install -r`, which goes through the shell and
      never shows the dialog. See README.

      RECEIVE_SMS is deliberately ABSENT. It is the permission Play Protect
      treats most harshly, and a ContentObserver plus the periodic sweep covers
      the same ground for a companion app. Medha removed it for exactly this
      reason after finding it bought nothing.

      There is NO INTERNET permission, and there must never be one. Loopback
      traffic to Medha does not require it. Its absence is a claim any user can
      verify in Settings, which is worth more than any privacy policy.
    -->
    <uses-permission android:name="android.permission.READ_SMS" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- Optional, P5. Omit for a cleaner permission story if replies deep-link
         to the stock messaging app instead. -->
    <uses-permission android:name="android.permission.READ_CONTACTS"
        tools:node="remove" />

    <application
        android:name=".SandeshikaApp"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:enableOnBackInvokedCallback="true"
        android:theme="@style/Theme.Sandeshika">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.Sandeshika">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver
            android:name=".ingest.BootReceiver"
            android:exported="true"
            android:enabled="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>
    </application>
</manifest>
