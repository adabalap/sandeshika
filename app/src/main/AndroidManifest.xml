<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
      INTERNET is the only permission this app declares.

      Sandeshika never holds READ_SMS. It asks Medha for messages over
      loopback, and Medha is the app that holds the permission and shows the
      user what it is doing with it. Splitting it that way means the app that
      renders your spending has no ability to read your inbox directly, and
      revoking Medha's access revokes it for everything at once.

      "Internet" here is 127.0.0.1. Android requires the permission for any
      socket, including one that never leaves the device; the network security
      config below is what actually constrains where it can go.
    -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".SandeshikaApp"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:supportsRtl="true"
        android:theme="@style/Theme.Sandeshika">

        <activity
            android:name=".MainActivity"
            android:configChanges="orientation|screenSize|keyboardHidden|uiMode"
            android:exported="true"
            android:launchMode="singleTask"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
