<?xml version="1.0" encoding="utf-8"?>
<!--
  Nothing leaves the device, including via backup. The database holds every
  SMS body; a cloud backup would silently undo the entire privacy posture.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="file" />
    </cloud-backup>
    <device-transfer>
        <exclude domain="root" />
    </device-transfer>
</data-extraction-rules>
