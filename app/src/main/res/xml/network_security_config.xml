<?xml version="1.0" encoding="utf-8"?>
<!--
  Cleartext is permitted to loopback and nowhere else.

  Medha listens on 127.0.0.1 over plain HTTP, which is fine: the traffic never
  leaves the device and never touches an interface. Everything else is denied,
  so a bug or an injected script cannot exfiltrate a transaction to a remote
  host in the clear — and with cleartext off globally, it cannot reach one at
  all without a valid certificate.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>

    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
</network-security-config>
