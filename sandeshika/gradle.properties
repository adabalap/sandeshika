# android.useAndroidX is mandatory, not a preference: AGP hard-fails at
# checkDebugAarMetadata the moment any AndroidX artifact appears without it.
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
kotlin.code.style=official
android.nonTransitiveRClass=true
